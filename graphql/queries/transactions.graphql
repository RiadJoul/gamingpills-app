query Transactions($skip: Float!, $take: Float!) {
  transactions(skip: $skip, take: $take) {
    transactions {
      id
      status
      type
      amount
      description
      createdAt
    }
    hasMore
  }
}
